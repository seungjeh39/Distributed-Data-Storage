public enum MerkleState {
    // Enum MerkleState class which represents the possible states of storage and the replicas
    INIT, READY, PROCESSING, PENDING, INVALID;

    @Override
    public String toString() {
        return super.toString();
    }
}

